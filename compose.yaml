version: '3.9'
services:
    redis:
        image: redis
        restart: always
        ports:
          - 6380:6379
        volumes:
            - redis_data_a:/data
        entrypoint: redis-server --appendonly yes

volumes:
    redis_data_a: